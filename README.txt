[!] Only files named plugin.* may be edited.
[!] No other frameworks/libs/plugins may be used.

1. Write a jQuery plugin that replaces <select> elements with a ordered list of <input>s and <label>s
2. The event delegation set up in main.js must still work after the plugin is applied.

Good luck and may the force be with you.